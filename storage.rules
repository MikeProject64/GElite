rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Regra de teste temporária para depurar o problema de upload.
    // NÃO USE EM PRODUÇÃO.
    match /serviceOrders/{orderId}/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}
