rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This rule allows any user to read and write to any document.
    // This is insecure for production but will unblock development.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
